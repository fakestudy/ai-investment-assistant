version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # StreamAnswerQuestion returns reusable AnswerChunk stream events.
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
